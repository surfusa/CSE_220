# For this particual instance makefile I only have one target,
# "helloworld.c"
# for this instance of compilation I'm using gcc, and there is a rule
# to make it known that a compile target is to be executed there must be
# a tab.
# finally there is also a "touch command" in the event that a force
# re-creation of an executable is necessary, and new time stamp is
# placed.

exec: hello
        ./hello

hello: helloworld.c
        gcc helloworld.c -o hello
        touch hello
